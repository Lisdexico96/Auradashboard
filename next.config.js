/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Enable static export so the app can be hosted on GitHub Pages
  output: "export",
};

module.exports = nextConfig;
